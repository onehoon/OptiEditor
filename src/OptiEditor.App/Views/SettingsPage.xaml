<Page x:Class="OptiEditor.App.Views.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="24">
        <Grid x:Name="SettingsHome">
            <StackPanel Spacing="16">
                <TextBlock Text="Settings" Style="{StaticResource TitleTextBlockStyle}" />
                <StackPanel x:Name="SettingsCategories" Spacing="8" />
            </StackPanel>
        </Grid>

        <Grid x:Name="EditorVisibilityDetail" Visibility="Collapsed" RowSpacing="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <StackPanel Spacing="8">
                <Button Content="&#xE72B;  Back" Click="Back_Click" HorizontalAlignment="Left" />
                <TextBlock Text="Editor visibility" Style="{StaticResource TitleTextBlockStyle}" />
                <TextBlock Text="Choose which OptiScaler settings appear when editing a game." Opacity="0.7" TextWrapping="Wrap" />
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Button Content="OptiScaler 0.9" Tag="V09" Click="Family_Click" />
                    <Button Content="OptiScaler 0.10" Tag="V10" Click="Family_Click" />
                    <Button Content="Save" Click="Save_Click" />
                    <Button Content="Reset defaults" Click="Reset_Click" />
                </StackPanel>
            </StackPanel>
            <TextBlock Grid.Row="1" Text="{x:Bind ViewModel.StatusText, Mode=OneWay}" TextWrapping="Wrap" />
            <ScrollViewer Grid.Row="2">
                <StackPanel x:Name="SettingsPanel" Spacing="4" />
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
